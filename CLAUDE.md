# 그린라이트 (노원구 대출 MVP) — 프로젝트 컨텍스트

> 이 파일은 새 세션이 맥락 없이도 이어받도록 정리한 인수인계 문서다.
> 코드 작업 전에 이 문서를 먼저 읽고, 아래 "가드레일"을 항상 지킬 것.

## 만드는 사람

- 현직 KB국민은행 대출상담 대리 + 전직 Java/Spring 백엔드 개발자.
- 선호: **본질 우선 > 기술적 화려함.** 카카오맵/캔버스/DB 같은 걸 성급히 끌어오지 말 것.
  화면의 정직한 감정 하나가 먹히는지부터 증명하는 게 목표.
- 솔직한 반대/지적을 환영함. 예스맨 금지.

## 이 서비스가 뭔가 (본질)

집을 알아보는 사람이 느끼는 핵심 감정은 "계약금 넣었다가 대출 안 나오면 어쩌지"라는
불안이다. 이 서비스의 본질은 **은행 앞에 불확실한 채로 설 필요 자체를 없애는 것** —
소득만 최소한 입력하면 "내가 살 수 있는 집"이 지도에 초록/노랑/회색으로 즉시 보인다.
심사받는 경험을 "이미 승인된 예산으로 쇼핑하는 경험"으로 뒤집는 게 핵심.

포지셔닝: 토스·핀다·당근은 조건 안 맞으면 "대출 불가" 팝업만 뱉는다.
우리는 **거절이 아니라 길을 알려주는, 현직 뱅커가 은행 내부를 솔직하게 알려주는 '내 편'**이다.
(비유: 토스=쇼핑몰, 우리=은행 가기 전 무기 보급고.)

## 타깃

서울·경기 2030 실수요층. MVP는 **서울 노원구, 아파트만** (KB시세 잡혀 담보 계산이 깨끗).
빌라/다세대/오피스텔은 MVP 밖(감정가가 안 나옴).

## 가드레일 (항상 지킬 것 — 절대 어기지 말 것)

1. **판정하지 말고 구간·가능성으로.** "3.2억 됩니다" 금지. "약 3~3.5억 가능성" O.
   단일 숫자로 확답하면 나중에 계약금 날린 고객의 원망이 우리에게 온다.
2. **"뚫는 법·치트키·협상 멘트" 금지. '예습'만.** "이렇게 말해서 뚫어라"가 아니라
   "은행이 이걸 보니 이걸 준비하고 이걸 물어봐라." 작업대출(허위서류)류는 절대 금지.
   (만드는 사람이 현직 은행원 → 이해상충·준법 리스크가 크다.)
3. **애매하면 노랑 = "상담역이 확정."** 정밀 대출 가부는 항상 상담사에게 넘긴다.
4. **거절로 문을 닫지 말 것.** 회색/닫힘에도 "이렇게 하면 열려요" 경로를 준다.
5. **프라이버시 = 기능.** 소득 등 입력값은 클라이언트를 벗어나지 않는다(서버 전송 X).

## 한도 계산 모델 (엔진의 뼈대)

주담대 한도 = **Min( LTV기반, DSR기반, 정부정책 규제, 상품별 cap )**, 단 자격 게이트를
모두 통과했을 때. 즉 "금액들의 Min"을 "자격들의 AND"가 감싸는 구조.
- LTV기반 = 담보가격 × LTV비율 − 방공제(소액임차보증금)
- DSR/DTI기반 = 소득·부채로 역산한 상환능력 한도
- 방공제는 MCI/MCG 가입으로 상쇄 가능(보금자리·은행) / 디딤돌은 대체로 상쇄 불가

## 아키텍처 (확정)

- **정적 프론트엔드 한 장. 백엔드·DB·Redis·배치 전부 없음.** (지금 단계)
- 데이터는 정적 JSON: 매물 시세(노원 한 동네 스냅샷) / 한도 룰셋 / 대처법 족보.
- 모든 계산은 브라우저에서. 시세 갱신 = JSON 갈아끼우고 재배포(라이브 파이프라인 X).
- 스택: React + Vite. `npm run dev`로 로컬 구동, Vercel/Netlify로 정적 배포.
- 서버/DB/카카오맵/PostGIS는 "리빌이 먹힌다"가 검증된 **한참 뒤** 얘기. 지금 짓지 말 것.

## 현재 코드 (파일 구조)

| 파일 | 역할 |
|---|---|
| `src/App.jsx` | 지도(예산) → 가능 매물 목록 → 매물선택 + 정부/은행 팝업. **단계 오케스트레이션 + 최상위 state** |
| `src/Eligibility.jsx` | **"자격 조건 입력" + "가능 상품 목록" 한 화면.** 자격을 묻는 유일한 곳 |
| `src/Strategy.jsx` | **조종간(인터랙티브 시뮬레이터).** 목표선 → 레버 2개 → 정직한 천장 |
| `src/DetailInfo.jsx` | **부채·소득의 질 입력** = 레버의 시작 위치를 잡는 첫 박자. Strategy 상단 |
| `src/engine.js` | 계산·판정 전부(중복 정의 금지). JSX 금지 |
| `src/data.js` | 매물·예산규칙·상품 **금융 파라미터**·`LEVER`(레버 범위)·화면 상수. 로직 금지 |
| `src/products/didimdol.js` | 디딤돌 5종 **자격 상한 규칙 데이터** |
| `src/products/bogeumjari.js` | 보금자리 규칙 데이터(같은 DSL) |
| `src/products/index.js` | `PRODUCT_RULES`로 합침 + 기금 공통 기준(신생아·신혼 기간) |
| `src/products/limit.js` | **레버값 → 한도 인터페이스**(`limitAt`/`ceilingAt` + 역산). 상품별 부채 잣대 |
| `src/ui.jsx` | 스타일 토큰 + 공용 레이아웃 + `useTween`. 상품별 분기 금지 |

**⚠️ 지금 모든 상품 숫자(금리·상한·cap·소득상한)는 임시 placeholder다. 실제 규제 값 아님.**

화면 흐름 (5단계):
1. **예산 화면**: 슬라이더(연소득·현금) → 지도에 단지가 초록/노랑/회색으로 켜짐(리빌).
   단지 탭 시 인라인 상세. 부채는 여기서 안 묻는다 — "천장"만 보여준다.
2. **"가능한 매물목록 확인하기"** → 매물 목록(초록·노랑, 가격순).
3. 목록에서 하나 선택 → **팝업**(위 정부상품 / 아래 은행상품, 트레이드오프 한 줄씩).
4. **자격 조건 입력 → 가능한 상품 목록**(`Eligibility.jsx`). 질문이 순서대로 열리고,
   답에 따라 불필요한 질문은 건너뛴다. 마지막에 **자격 되는 상품을 전부 카드로** 보여준다
   (상품명·대략 한도·왜 가능한지). **부채 입력 전에 뜬다** — 이 목록 자체가 두 번째 리빌.
   은행탭은 아직 placeholder("곧 준비").
5. 카드 하나 선택 → **조종간**(`Strategy.jsx`). 아래 "조종간" 절 참고.

## 조종간 (Strategy) — 이 서비스 정체성이 가장 진한 화면

**대출을 '조회'해주는 게 아니라, 원하는 집에 닿도록 '레버'를 쥐여준다.**
그래서 한도는 고정 결과가 아니라 사용자가 당기면 실시간으로 반응하는 값이다.

위→아래 3층 + 번역:
1. **목표선** — 고른 집의 필요 대출액이 고정 기준선. 지금 닿는 지점과 간극을 항상 같이 보여준다.
   기준선은 안 움직이고 레버가 움직인다(그래야 "내가 움직인다"가 전달된다).
2. **레버 2개** — 부채 레버(왼쪽 = 일부상환 가정) / 소득 레버(오른쪽 = 인정소득 상향 가정).
   2박자: DetailInfo에서 '대충' 받은 값이 **레버의 시작 위치**가 되고, 곧바로 손잡이가 살짝 튀며
   "당겨보세요"로 유도한다. 소득 레버 옆에는 **소득의 질** 배지(초록=신뢰 / 노랑=상담역 확인)를 붙이고,
   노랑이면 결과 색도 노랑으로 물든다.
3. **정직한 천장** — 두 레버를 끝까지 당겼을 때의 최대선. 그 위는 "레버로도 지금은 무리"라고 긋는다.
4. **행동 번역** — 레버 끝에는 항상 실제 행동을 붙인다. "= 신용대출 약 X 일부상환(일부상환조건부)",
   "= 인정소득 약 X 확인 필요 → 상담역". 시뮬레이션에서 멈추지 않는다. 가드레일 2대로 **예습만**.

### 조종간이 지켜야 하는 정직함
- **레버는 자격도 움직인다.** 소득 레버를 올리면 소득상한을 넘어 상품이 **닫힐** 수 있다.
  그래서 소득 레버 최대는 그 경로의 소득상한에서 자르고, 닫히는 상품은 목록에 명시한다.
  소득을 올리는 게 항상 유리하지 않다는 게 이 화면의 도메인 지식이다.
- **레버가 안 먹히는 구간을 숨기지 않는다.** 벽이 담보(LTV)나 경로 한도면 부채·소득으로는
  안 내려간다. 그럴 때 `leversInert`로 잡아 "이 경로에선 레버가 결과를 못 움직여요"를 먼저 말하고
  열려 있는 길(현금 차액 / 다른 경로)로 넘긴다. **움직이는 척하면 거짓 희망 기계가 된다.**
- **보유 현금은 조종간에서 다시 묻지 않는다.** 부족분은 "차액 약 X를 현금으로 채우면 돼요"로만 번역한다.

### 레버 → 결과 파이프
```
DetailInfo(대충 입력) → base(레버 시작 위치) → pull(당긴 값) → lever
   lever ─→ products/limit.js  limitAt(productKey, lever, capOverride) ─→ { limit, parts, binding }
                               ceilingAt(...)                          ─→ 정직한 천장
                               creditLoanToReach / incomeToReach       ─→ 행동 번역용 역산(이분탐색)
   lever.income ─→ engine.withAssumedIncome(ctx) → deriveFacts → judgeAll ─→ 자격 재판정(순위 반응)
```
Strategy는 **어떤 상품인지 몰라도 된다** — `productKey`와 레버 값만 넘긴다.
상품 고유 지식(부채를 이자만 보는가 원리금으로 보는가)은 `products/limit.js`의 `DEBT_VIEW`에만 있다.

### 화면 경계 원칙 (이걸 어기면 예전의 중복으로 되돌아간다)
- **자격은 `Eligibility` 한 곳에서만 받는다.** Strategy·products는 자격을 다시 묻지 않는다.
  자격 질문이 더 필요해지면 `EMPTY_ELIG`에 필드를 추가하고 Eligibility에 UI를 단다.
- **부채·소득의 질은 `DetailInfo` 한 곳에서만 받는다.** 자격 화면에는 절대 올리지 않는다
  — 자격은 소득상한 위/아래로 갈리지 부채로 갈리지 않기 때문.
- 컨텍스트는 `engine.buildCtx()`가 만든 객체 하나(`ctx`)로만 흐른다. 화면이 상태를 복제하지 않는다.
- 자격 판정 결과는 들고 다니지 않고 `deriveFacts(ctx) → judgeAll(facts)`로 그때그때 다시 계산한다.

## 정부탭 시나리오 로직 (도메인 — 만드는 사람의 현직 지식)

- 진입 게이트 = **무주택**.
- 가장 결정적 입력 = **부부합산소득** (DTI로 실질 한도 결정. 고객은 다 70% 원하지만 실제론 안 됨).
  소득 상한은 **신혼 여부·미성년 자녀 수**에 따라 달라짐 → 그래서 자녀 생년월일까지 받음.
- **디딤돌 vs 보금자리는 고객에게 묻지 않고 계산한다.** 단 "하나로 좁혀서" 추천하지 않는다 —
  자격 되는 건 전부 목록에 올리고, 어느 걸 택할지는 한도·금리·실행시점을 같이 보고 정한다.
  "방공제 필요하세요?"는 질문이 아니라 **계산 결과(binding)로 보여준다.**
- **실행 시점(시간)이 정부 vs 은행을 가르는 결정적 축.** 정부상품은 실행 ~2개월 →
  잔금일 못 맞추면 계약금 사고. 카드에 "한도 / 금리 / 실행시점" 3줄로 보여줄 것.

## 판정 엔진 구조 (상품이 늘어도 코드는 안 고친다)

`src/products/*`의 규칙 배열은 **플래그 이름으로 쓴 조건 DSL**이다 — 데이터에 함수를 두지 않는다.
```
[]                → 항상 참(기본 티어)   ["a","b"] → a AND b   ["a",["b","c"]] → a AND (b OR c)
```
플래그는 `engine.deriveFacts(ctx)`가 만든다. 새 조건이 필요하면 **거기에 플래그를 추가**하고
규칙 데이터에서 이름으로 참조한다. 판정 함수(`judgeRule`/`judgeAll`)는 손대지 않는다.
`incomeCap`/`priceCap`/`loanCap`은 조건별 티어 배열이고 **위에서부터 첫 매치**가 적용된다(유리한 걸 위로).
판정은 실패해도 `unmetRequires`·`failedChecks`(무엇이 얼마 초과)를 항상 같이 돌려준다 — 가드레일 4.

## 다음 할 일 (TODO)

1. **⚠️ 최우선 — DTI 잣대와 경로 한도의 스케일이 안 맞는다.**
   지금 `PRODUCTS.ratio=0.60` + 30년 + `calcRate`로 계산하면 DTI 한도 ≈ **연소득 × 11.9배**가 나온다.
   그래서 연소득 약 2,000만원 미만이 아니면 DTI가 절대 벽이 되지 않고, 항상 경로 한도나 LTV가 먼저 걸린다
   → **부채 레버가 결과를 못 움직인다**(조종간은 이걸 `leversInert`로 정직하게 표시하지만, 레버 자체가 죽는다).
   실제 기금 DTI 심사식(가산금리·만기·인정 원리금 계산)으로 교체해야 조종간이 제 값을 한다.
2. `src/products/*`의 가상 숫자를 **실제 디딤돌/보금자리 규제 값**으로 교체(소득·가격·면적 상한, 한도),
   `data.PRODUCTS`의 금리·LTV·cap·실행시점, `products/limit.js`의 `DEBT_VIEW`도 실제 관행으로.
3. **본격 비교** — 순위 반응 섹션에 금리·총이자·실행시점 축 추가(지금은 한도 축만).
4. 은행탭 로직 구현(일반 주담대, MCI 방공제 상쇄, DSR 40%, 실행 빠름) → `products/bank.js` +
   `DEBT_VIEW.bank = "principalAndInterest"` + 탭 연결.
5. 노원 실거래가 6개 추정값을 실제 값으로 교체.
6. 소득의 질(노랑)을 **문구만이 아니라 숫자에도** 반영할지 결정 — 지금은 색만 물들이고 한도는 안 깎는다.
   깎으려면 인정률 가정이 필요한데 그건 가상값을 하나 더 만드는 일이라 일부러 보류했다.

## 실행 방법

최초 1회: `npm create vite@latest greenlight -- --template react` → `cd greenlight` →
`npm install` → 이 프로젝트의 App.jsx를 `src/App.jsx`에 덮어쓰기.
이후: `npm run dev` → 뜨는 `localhost:5173` 열기. 배포는 `npm run build` 후 dist를 Vercel/Netlify.
